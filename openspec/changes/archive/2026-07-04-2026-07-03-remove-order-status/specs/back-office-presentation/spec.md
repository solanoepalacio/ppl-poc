## REMOVED Requirements

### Requirement: Order status control is labelled, not color-coded
**Reason**: Order status is removed, so there is no status control to label or de-color-code; the `Estado` selector and its status dot are gone from the order card.
**Migration**: The `OrderStatusControl` and its `Estado` label/dot are deleted from the back office; no status presentation remains.
