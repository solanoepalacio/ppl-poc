import { ProductionView } from '../ProductionView';

/** Production totals for the savory line (*salados*), for the open bloque. */
export default function ProductionSaladosPage() {
  return <ProductionView category="salty" />;
}
